<%= form_for(@presupuesto_detalle) do |f| %>
  <% if @presupuesto_detalle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presupuesto_detalle.errors.count, "error") %> prohibited this presupuesto_detalle from being saved:</h2>

      <ul>
      <% @presupuesto_detalle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :presupuesto_id %><br>
    <%= f.number_field :presupuesto_id %>
  </div>
  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
